import { ENVIRONMENT } from '@/constants/environment';
export const GOOGLE_OAUTH2_URL = 'https://accounts.google.com/o/oauth2/v2/auth';

export const GATHER_OAUTH_URL = ENVIRONMENT.GATHER_OAUTH_URL;

export const GATHER_OAUTH_WINDOW_WIDTH = 1000;
export const GATHER_OAUTH_WINDOW_HEIGHT = 750;
